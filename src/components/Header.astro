---
import Link from '@components/HeaderLink.astro';

const navLinks = [
	{name: 'Home', url: '/'},
];
---

<header>
	<img href="favicon.svg" />
	<nav>
		{navLinks.map(link => <Link url={link.url} name={link.name} />)}
	</nav>
</header>

<style lang="scss">
header {
	padding: 0 12px;
	display: flex;
	justify-content: space-between;
	img {
		width: 14px;
		height: 14px;
	}
	nav {
		display: flex;
		font: 1em sans-serif;
		padding: 0 8px;
		justify-content: center;
		align-items: stretch;
	}
}
</style>
